export type PaginationHeaders = {
  pageSize: number;
  totalPage: number;
  totalSize: number;
  currentPage: number;
};

export type PaginationParams = {
  pageSize: number;
  pageNumber: number;
};
